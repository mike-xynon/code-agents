# Claude Worker Farm Environment Configuration

# Anthropic API Key (required for Claude Code to work)
ANTHROPIC_API_KEY=your-api-key-here

# Host address for accessing the dashboard
# Use 'localhost' for local development
# Use your server's IP or domain for remote access
NGINX_HOST=localhost

# Optional: Redis password (uncomment to enable)
# REDIS_PASSWORD=your-redis-password

# Optional: Maximum number of workers
# MAX_WORKERS=20

# Optional: Base port for worker containers
# BASE_PORT=7681
